function drawTriangle(numRows, numCols, boxChar) {
   for (let i = 0; i < numRows; i++){
      let boxChar = "";
      for (let j = 0; j <  numCols; j++){
         boxChar = "*";
      }
   }
   console.log(boxChar);
}

drawTriangle(4, 4);
